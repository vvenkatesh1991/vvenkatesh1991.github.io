// This model defines a single book mark entry
define(['jQuery', 'underscore', 'backbone'], function($, _, Backbone) {
	var bookmarkModel = Backbone.Model.extend({
		
	});
	
	return bookmarkModel;
});